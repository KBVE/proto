# Schema changes fan out to every consumer and are breaking by default, so
# everything here requires review.
*                     @h0lybyte

# The shared layers are imported by every domain: a change here is a change to
# all of them.
/kbve/type/           @h0lybyte
/kbve/common/         @h0lybyte

# Tooling and release configuration.
/buf.yaml             @h0lybyte
/buf.gen.yaml         @h0lybyte
/.github/             @h0lybyte
